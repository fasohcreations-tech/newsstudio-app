/**
 * @deprecated Prefer `@/features/scene-composer/lib/media-container`.
 * Kept so existing background imports keep working.
 */

export {
  looksLikeBackgroundImageUrl,
  parseBackgroundSlides,
  resolveBackgroundSlideControls,
  type BackgroundMediaFit,
  type BackgroundSlideControls,
  type BackgroundTransitionStyle,
} from "@/features/scene-composer/lib/media-container";
